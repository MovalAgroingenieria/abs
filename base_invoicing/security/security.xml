<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="group_account_invoiceset_config" model="res.groups">
        <field name="name">Billing Configurator</field>
        <field name="category_id"
            ref="base.module_category_accounting_accounting"/>
        <field name="implied_ids"
            eval="[(4, ref('account.group_account_manager'))]"/>
        <field name="users"
            eval="[(4, ref('base.user_root')),
                   (4, ref('base.user_admin'))]"/>
    </record>

</odoo>
