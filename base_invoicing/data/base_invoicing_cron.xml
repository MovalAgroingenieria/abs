<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <record id="account_invoiceset_cron_calculate_all_configured_invoiceset" model="ir.cron">
        <field name="name">Invoice Sets: Calculate all configured invoice sets</field>
        <field name="model_id" ref="base_invoicing.model_account_invoiceset"/>
        <field name="state">code</field>
        <field name="code">model.calculate_all_configured_invoiceset()</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active" eval="False"/>
    </record>

</odoo>